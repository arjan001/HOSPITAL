// Email is sent server-side via the API server.
// This stub exists so any legacy import does not break the frontend bundle.

export async function sendOrderConfirmationEmail(_data: unknown): Promise<boolean> {
  return false
}
